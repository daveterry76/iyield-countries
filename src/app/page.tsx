import { Container, Typography } from "@mui/material";
import type { FC } from "react";

const HomePage: FC = () => {
  return (
    <Container>
      <Typography variant="h3">Country Search</Typography>
      <em>Render your component here.</em>
    </Container>
  );
};

export default HomePage;
